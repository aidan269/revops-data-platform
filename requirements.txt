# Raw-data ingestion platform — only what retained code imports.
psycopg2-binary>=2.9,<3   # extract/*.py, scripts/run_db_migrations.py
requests>=2.31,<3         # read-only HubSpot GET calls in extract/*.py
