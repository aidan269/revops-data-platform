psycopg2-binary>=2.9,<3
requests>=2.31,<3
PyYAML>=6.0,<7
# MCP stdio server (hermes/codex_mcp.py — bridges Codex <-> host Ollama LLM)
# Pinned to 1.x: uses mcp.server.fastmcp FastMCP API with transport='stdio'.
mcp>=1.28,<2
